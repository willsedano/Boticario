base: --no-source --color --format pretty
default: --profile qa
qa: --profile base PROFILE=qa
preprod: --profile base PROFILE=preprod
prod: --profile base PROFILE=prod
local: --profile base PROFILE=local


html_report: --format progress --format html --out=features_report.html  
bvt: --tags @bvt
json_report: --format progress --format json --out=features_format.json
